<%= error_messages_for @pages %> 

 <%= link_to "<< back to list" , {:action => 'index'}, :class => 'back_link' %>
 <div class="page new">
 	<h2>Create Page</h2>
 	<%= form_for(:pages, :url => { :action => 'create' } ) do |f| %>
 		<table summary="page form field">
 			<tr>
 				<th><%= f.label :subject_id, "Subject " %></th>
 				<td><%= f.select(:subject_id,@subjects.map { |s| [s.name ,s.id]  }) %></td>
 			</tr>
 			<tr>
 				<th><%= f.label :name, "Name" %></th>
 				<td><%= f.text_field(:name) %></td>
 			</tr>
 			<tr>
 				<th><%= f.label :permalink, "Permalink" %></th>
 				<td><%= f.text_field(:permalink) %></td>
 			</tr>
 			<tr>
 				<th><%= f.label :position, "Position" %></th>
 				<td><%= f.select(:position, 1..@page_count) %></td>
 			</tr>
 			<tr>
 				<th><%= f.label :visible, "Visible" %></th>
 				<td><%= f.check_box :visible %></td>
 			</tr>
 		</table>
 		<div class="form buttons">
 			<%= submit_tag("Create Page")%>
 		</div>
 	<% end %>
 </div>

 