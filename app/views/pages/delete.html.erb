
<%= link_to "<< back to list" , {:action => 'index', :subject_id => @subject.id}, :class => 'back_link' %>
<div class="page delete">
	<h2>Page deletes</h2>

	<%= form_for(:pages, :url => { :action => 'destroy', :id => @pages.id, :subject_id => @subject.id } ) do |f| %>
		<p>are you sure you want delete this Page?</p>
		<p class="reference-name"> <%= @pages.name%></p>
		<div class="form-buttons">
			<%= submit_tag("Delete Page")%>
		</div>
	<% end %>
</div>